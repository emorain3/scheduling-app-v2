const defaultTimeWindow = ["9am", "10am", "11am", "12pm", "1pm", "2pm", "3pm", "4pm", "5pm"];

let timeWindowReducer = (state = defaultTimeWindow, action) => {
    switch (action.type) {
        default: return state
    }
    
  }

  export default timeWindowReducer;